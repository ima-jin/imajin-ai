{
  "name": "@imajin/claw-provisioner",
  "version": "0.1.0",
  "private": true,
  "description": "Operator-executed runner for the envelope provisioner (imajin-ai#1933): consumes a kernel provision record, materializes its rendered envelope, and (for hosted placements, non-dry-run only) runs the deploy/nanoclaw compose stack, reporting boot status back to the kernel. Never touches live infra by itself in this repo's tests/CI.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "claw-provisioner": "./dist/cli.js"
  },
  "files": [
    "dist/",
    "src/"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "run": "tsx src/cli.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@imajin/claw-envelope": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5",
    "vitest": "^2.1.9"
  }
}
