name: Build CI Image

on:
  push:
    branches: [main]
    paths:
      - '.github/docker/Dockerfile.ci'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/ci-runner

jobs:
  build-and-push:
    name: Build & Push CI Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to Container registry
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef # v3.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@ee4ca427a2f43b6a16632044ca514c076267da23 # v6.19.0
        with:
          context: .github/docker
          file: .github/docker/Dockerfile.ci
          push: true
          # `:latest` stays for local/manual convenience, but CI jobs in
          # ci.yml pin to the immutable `:<sha>` tag (githubactions:S6596 —
          # image references must use a specific version, not a floating
          # tag). Bump the sha ci.yml references whenever this Dockerfile
          # changes and this workflow has re-run on main.
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
