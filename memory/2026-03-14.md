# 2026-03-14 — Pay Settlement UI

## What was built

### #143 — Two-Bucket Balance Display
- `apps/pay/app/components/BalanceCard.tsx` — balance card showing cash (withdrawable from Stripe) and credit (spend-only) buckets, plus total

### #335 — Payment History UI
- `apps/pay/app/page.tsx` — dashboard page replacing static API docs; requires auth, shows balance + last 5 transactions
- `apps/pay/app/history/page.tsx` — full paginated history with server-rendered filters (service, date range), expandable rows showing .fair manifest chain + metadata

### #337 — Platform Fee in .fair Settlement (verified, no changes needed)
- `events/src/lib/settle.ts` already appends platform DID at 3% to the chain
- `pay/app/api/settle/route.ts` correctly credits all chain entries including platform
- Format mismatch check: events and pay are aligned on `{did, amount, role}` chain format with `funded: true`

### Settlement Silent Skip Fix (no changes needed)
- Prior webhook crash was the Date serialization bug (`fix: serialize Date to ISO string in onboard token insert`)
- End-to-end flow is correct; chain total validation passes after rounding drift correction in settle.ts
