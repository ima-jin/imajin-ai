'use client'; import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { ImageUpload } from '@/app/components/ImageUpload';
import { MarkdownEditor } from '@imajin/ui';
import { apiFetch } from '@imajin/config'; interface Props { organizerDid: string;
} interface TicketTier { name: string; price: number; quantity: number | null; description: string;
} export default function EventCreateForm({ organizerDid }: Props) { const router = useRouter(); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); // Basic info const [name, setName] = useState(''); const [tagline, setTagline] = useState(''); const [description, setDescription] = useState(''); const [dateTime, setDateTime] = useState(''); const [endDateTime, setEndDateTime] = useState(''); const [locationType, setLocationType] = useState<'physical' | 'virtual' | 'hybrid'>('physical'); const [virtualUrl, setVirtualUrl] = useState(''); const [venue, setVenue] = useState(''); const [address, setAddress] = useState(''); const [city, setCity] = useState(''); const [country, setCountry] = useState(''); const [coverImageUrl, setCoverImageUrl] = useState(''); const [courseSlug, setCourseSlug] = useState(''); // Ticket tiers const [tiers, setTiers] = useState<TicketTier[]>([ { name: 'General Admission', price: 0, quantity: null, description: '' } ]); function addTier() { setTiers([...tiers, { name: '', price: 0, quantity: null, description: '' }]); } function updateTier(index: number, field: keyof TicketTier, value: any) { const newTiers = [...tiers]; newTiers[index] = { ...newTiers[index], [field]: value }; setTiers(newTiers); } function removeTier(index: number) { if (tiers.length > 1) { setTiers(tiers.filter((_, i) => i !== index)); } } async function handleSubmit(e: React.FormEvent) { e.preventDefault(); setLoading(true); setError(''); try { const response = await apiFetch('/api/events', { method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include', // Include cookies for auth body: JSON.stringify({ title: name, description: `${tagline ? tagline + '\n\n' : ''}${description}`, startsAt: new Date(dateTime).toISOString(), endsAt: endDateTime ? new Date(endDateTime).toISOString() : null, locationType, isVirtual: locationType !== 'physical', virtualUrl: locationType !== 'physical' ? virtualUrl : null, venue: locationType !== 'virtual' ? venue : null, address: locationType !== 'virtual' ? address : null, city: locationType !== 'virtual' ? city : null, country: locationType !== 'virtual' ? country : null, imageUrl: coverImageUrl || null, courseSlug: courseSlug || null, tickets: tiers.filter(t => t.name).map(t => ({ name: t.name, description: t.description, price: Math.round(t.price * 100), // Convert to cents currency: 'CAD', quantity: t.quantity, })), }), }); if (!response.ok) { const data = await response.json(); throw new Error(data.error || 'Failed to create event'); } const data = await response.json(); router.push(`/${data.event.id}`); } catch (err) { setError(err instanceof Error ? err.message : 'Failed to create event'); } finally { setLoading(false); } } return ( <form onSubmit={handleSubmit} className="space-y-6"> {/* Basic Info */} <div className="bg-surface-surface bg-surface-elevated p-6 space-y-4"> <h2 className="text-xl font-mono font-semibold">Basic Info</h2> <div> <label className="block font-mono text-sm font-medium mb-1">Event Name *</label> <input type="text" value={name} onChange={(e) => setName(e.target.value)} required placeholder="Jin's Launch Party" className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> <div> <label className="block font-mono text-sm font-medium mb-1">Tagline</label> <input type="text" value={tagline} onChange={(e) => setTagline(e.target.value)} placeholder="A night of light, sound, and presence" className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> <div className="grid grid-cols-1 md:grid-cols-2 gap-4"> <div> <label className="block font-mono text-sm font-medium mb-1">Start Date & Time *</label> <input type="datetime-local" value={dateTime} onChange={(e) => setDateTime(e.target.value)} required className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> <div> <label className="block font-mono text-sm font-medium mb-1">End Date & Time</label> <input type="datetime-local" value={endDateTime} onChange={(e) => setEndDateTime(e.target.value)} className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> </div> <div className="flex border border-white/10 border-white/10 overflow-hidden"> {(['physical', 'virtual', 'hybrid'] as const).map((type) => ( <button key={type} type="button" onClick={() => setLocationType(type)} className={`flex-1 py-2 text-sm font-medium transition ${ locationType === type ? 'bg-imajin-orange text-primary' : 'bg-surface-surface bg-surface-elevated text-primary text-primary hover:bg-surface-elevated hover:bg-surface-elevated' }`} > {type.charAt(0).toUpperCase() + type.slice(1)} </button> ))} </div> {locationType !== 'physical' && ( <div> <label className="block font-mono text-sm font-medium mb-1">Virtual URL</label> <input type="url" value={virtualUrl} onChange={(e) => setVirtualUrl(e.target.value)} placeholder="https://zoom.us/j/..." className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> )} {locationType !== 'virtual' && ( <> <div> <label className="block font-mono text-sm font-medium mb-1">Venue</label> <input type="text" value={venue} onChange={(e) => setVenue(e.target.value)} placeholder="Event Space Name" className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> <div> <label className="block font-mono text-sm font-medium mb-1">Address</label> <input type="text" value={address} onChange={(e) => setAddress(e.target.value)} placeholder="123 Main St" className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> <div className="grid grid-cols-2 gap-4"> <div> <label className="block font-mono text-sm font-medium mb-1">City</label> <input type="text" value={city} onChange={(e) => setCity(e.target.value)} placeholder="Toronto" className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> <div> <label className="block font-mono text-sm font-medium mb-1">Country</label> <input type="text" value={country} onChange={(e) => setCountry(e.target.value)} placeholder="Canada" className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> </div> </> )} <div> <label className="block font-mono text-sm font-medium mb-1">Linked Course Slug</label> <input type="text" value={courseSlug} onChange={(e) => setCourseSlug(e.target.value)} placeholder="intro-to-ai" className="w-full px-4 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> <p className="text-xs text-secondary mt-1">Optional: link this event to a course on Learn</p> </div> <ImageUpload onUploadComplete={(url) => setCoverImageUrl(url)} /> <div> <label className="block font-mono text-sm font-medium mb-1">Description</label> <MarkdownEditor value={description} onChange={setDescription} placeholder="Tell people what your event is about..." /> </div> </div> {/* Ticket Tiers */} <div className="bg-surface-surface bg-surface-elevated p-6 space-y-4"> <div className="flex justify-between items-center"> <h2 className="text-xl font-mono font-semibold">Tickets</h2> <button type="button" onClick={addTier} className="text-imajin-orange hover:text-imajin-orange font-mono text-sm font-medium" > + Add Tier </button> </div> {tiers.map((tier, index) => ( <div key={index} className="border border-white/10 border-white/10 p-4 space-y-3"> <div className="flex justify-between items-start"> <div className="flex-1 grid grid-cols-2 gap-3"> <div> <label className="block font-mono text-sm font-medium mb-1">Tier Name</label> <input type="text" value={tier.name} onChange={(e) => updateTier(index, 'name', e.target.value)} placeholder="General Admission" className="w-full px-3 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated text-sm" /> </div> <div> <label className="block font-mono text-sm font-medium mb-1">Price (CAD)</label> <input type="number" min="0" step="0.01" value={tier.price} onChange={(e) => updateTier(index, 'price', parseFloat(e.target.value) || 0)} className="w-full px-3 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated text-sm" /> </div> <div> <label className="block font-mono text-sm font-medium mb-1">Quantity (blank = unlimited)</label> <input type="number" min="1" value={tier.quantity || ''} onChange={(e) => updateTier(index, 'quantity', e.target.value ? parseInt(e.target.value) : null)} placeholder="Unlimited" className="w-full px-3 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated text-sm" /> </div> <div> <label className="block font-mono text-sm font-medium mb-1">Description</label> <input type="text" value={tier.description} onChange={(e) => updateTier(index, 'description', e.target.value)} placeholder="What's included" className="w-full px-3 py-2 border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated text-sm" /> </div> </div> {tiers.length > 1 && ( <button type="button" onClick={() => removeTier(index)} className="ml-3 text-error hover:text-error text-sm" > Remove </button> )} </div> </div> ))} </div> {error && ( <div className="p-4 bg-red-50 dark:bg-error/10 text-error text-error"> {error} </div> )} <button type="submit" disabled={loading} className="w-full py-3 bg-imajin-orange hover:bg-imajin-orange disabled:bg-orange-300 text-primary font-semibold transition" > {loading ? 'Creating...' : 'Create Event'} </button> </form> );
}
