'use client'; import Link from 'next/link'; interface EventWithStats { id: string; title: string; startsAt: Date; imageUrl: string | null; status: string; statusBadge: string; ticketsSold: number; revenue: number;
} interface Props { event: EventWithStats;
} const statusColors = { draft: 'bg-surface-elevated text-primary bg-surface-elevated text-primary', live: 'bg-success/10 text-success dark:bg-success/10 text-success', paused: 'bg-warning/10 text-warning dark:bg-warning/10 text-warning', past: 'bg-info/10 text-info dark:bg-info/10 dark:text-info', cancelled: 'bg-error/10 text-error dark:bg-error/10 text-error',
}; const statusLabels = { draft: 'Draft', live: 'Live', paused: 'Paused', past: 'Past', published: 'Live', cancelled: 'Cancelled', completed: 'Past',
}; export function EventCard({ event }: Props) { const eventDate = new Date(event.startsAt); const formattedDate = eventDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric', }); const formattedTime = eventDate.toLocaleTimeString('en-US', { hour: 'numeric', minute: '2-digit', }); const status = event.statusBadge || event.status; const statusColor = statusColors[status as keyof typeof statusColors] || statusColors.draft; const statusLabel = statusLabels[status as keyof typeof statusLabels] || status; return ( <div className="bg-surface-surface bg-surface-elevated/50 backdrop-blur-sm overflow-hidden hover: transition- group"> {/* Image */} <Link href={`/${event.id}`} className="block"> <div className="relative h-48 bg-gradient-to-br from-imajin-orange to-imajin-purple overflow-hidden"> {event.imageUrl ? ( <img src={event.imageUrl} alt={event.title} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300" /> ) : ( <div className="w-full h-full flex items-center justify-center"> <span className="text-6xl">🎉</span> </div> )} {/* Status badge */} <div className="absolute top-3 right-3"> <span className={`px-3 py-1 rounded-full text-xs font-semibold uppercase ${statusColor}`}> {statusLabel} </span> </div> </div> </Link> {/* Content */} <div className="p-5"> <Link href={`/${event.id}`} className="block mb-3"> <h3 className="text-xl font-mono font-bold group-hover:text-imajin-orange transition-colors line-clamp-2"> {event.title} </h3> </Link> <div className="flex items-center gap-2 text-sm text-muted text-secondary mb-4"> <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" /> </svg> <span>{formattedDate} at {formattedTime}</span> </div> {/* Stats */} <div className="grid grid-cols-2 gap-4 pt-4 border-t border-white/10 border-white/10"> <div> <div className="text-xs text-secondary text-secondary mb-1">Tickets Sold</div> <div className="text-2xl font-mono font-bold">{event.ticketsSold}</div> </div> <div> <div className="text-xs text-secondary text-secondary mb-1">Revenue</div> <div className="text-2xl font-mono font-bold"> CA\$${(event.revenue / 100).toFixed(0)} </div> </div> </div> {/* Actions */} <div className="mt-4 flex gap-2"> <Link href={`/${event.id}/edit`} className="flex-1 text-center px-4 py-2 bg-surface-elevated hover:bg-surface-elevated bg-surface-elevated hover:bg-surface-elevated text-primary text-primary font-medium transition-colors" > Edit </Link> <Link href={`/${event.id}`} className="flex-1 text-center px-4 py-2 bg-imajin-orange hover:bg-imajin-orange text-primary font-medium transition-colors" > View </Link> </div> </div> </div> );
}
