'use client'; import { useState, useEffect } from 'react';
import { apiFetch } from '@imajin/config'; interface EventChatButtonProps { eventId: string; chatUrl?: string;
} export function EventChatButton({ eventId, chatUrl }: EventChatButtonProps) { const [conversationId, setConversationId] = useState<string | null>(null); const [loading, setLoading] = useState(true); const baseChatUrl = chatUrl || (typeof window !== 'undefined' ? `${window.location.protocol}//${window.location.hostname.replace('events', 'chat')}` : 'https://chat.imajin.ai'); useEffect(() => { async function checkAccess() { try { const res = await apiFetch(`/api/events/${eventId}/my-ticket`); if (res.ok) { const data = await res.json(); if (data.hasTicket) { setConversationId(data.conversationId); } } } catch (err) { console.error('Failed to check event access:', err); } finally { setLoading(false); } } checkAccess(); }, [eventId]); if (loading || !conversationId) return null; return ( <div className="grid grid-cols-1 gap-4"> {/* Private Event Chat - Pod members only */} {conversationId && ( <a href={`${baseChatUrl}/conversations/${conversationId}`} className="px-6 py-4 bg-gradient-to-r from-imajin-purple to-imajin-blue text-primary font-semibold hover:from-purple-600 hover:to-indigo-700 transition flex flex-col items-center justify-center gap-2" > <span className="text-2xl">🔒</span> <span>Private Chat</span> <span className="text-xs opacity-90">Encrypted group chat</span> </a> )} </div> );
}
