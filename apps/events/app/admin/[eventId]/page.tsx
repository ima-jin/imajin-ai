/** * Admin view for event ticket management * /admin/[eventId] */ import { db, events, tickets, ticketTypes } from '@/src/db';
import { eq, desc } from 'drizzle-orm';
import { notFound } from 'next/navigation';
import { EventStatusControls } from './event-status-controls';
import { CohostManager } from './cohost-manager';
import { GuestList } from './guest-list';
import { InviteManager } from './invite-manager';
import { MessageComposer } from './message-composer';
import { getSession } from '@imajin/auth';
import { getClient } from '@imajin/db'; const sql = getClient(); interface Props { params: { eventId: string };
} export default async function AdminPage({ params }: Props) { const { eventId } = params as unknown as { eventId: string }; // Fetch event const [event] = await db .select() .from(events) .where(eq(events.id, eventId)) .limit(1); if (!event) { notFound(); } // Auth check: viewer must be owner or cohost const session = await getSession(); const isOwner = session?.id === event.creatorDid; if (!isOwner && event.podId) { // Check if viewer is a cohost const podRows = await sql` SELECT role FROM connections.pod_members WHERE pod_id = ${event.podId} AND did = ${session?.id ?? ''} AND role IN ('owner', 'cohost') LIMIT 1 `; if (!podRows.length) { notFound(); } } else if (!isOwner) { notFound(); } // Fetch ticket types with stats const tiers = await db .select() .from(ticketTypes) .where(eq(ticketTypes.eventId, eventId)); // Fetch all tickets const allTickets = await db .select({ ticket: tickets, tierName: ticketTypes.name, }) .from(tickets) .leftJoin(ticketTypes, eq(tickets.ticketTypeId, ticketTypes.id)) .where(eq(tickets.eventId, eventId)) .orderBy(desc(tickets.createdAt)); // Calculate stats — exclude cancelled/refunded from"sold" count const activeTickets = allTickets.filter(t => !['cancelled', 'refunded'].includes(t.ticket.status)); const totalSold = activeTickets.length; const confirmedRevenue = allTickets .filter(t => t.ticket.status === 'valid' || t.ticket.status === 'used') .reduce((sum, t) => sum + (t.ticket.pricePaid || 0), 0); const heldRevenue = allTickets .filter(t => t.ticket.status === 'held') .reduce((sum, t) => sum + (t.ticket.pricePaid || 0), 0); const checkedIn = allTickets.filter(t => t.ticket.usedAt).length; // Count distinct confirmed attendees for broadcast const confirmedAttendeeRows = await sql` SELECT COUNT(DISTINCT owner_did) AS count FROM events.tickets WHERE event_id = ${eventId} AND status IN ('valid', 'used') AND owner_did IS NOT NULL `; const confirmedAttendeeCount = Number(confirmedAttendeeRows[0]?.count ?? 0); return ( <div className="max-w-6xl mx-auto p-4 md:p-8"> {/* Header */} <div className="mb-4 md:mb-8"> <h1 className="text-3xl font-mono font-bold">{event.title}</h1> <p className="text-muted text-secondary">Admin Dashboard</p> </div> {/* Status Controls */} <EventStatusControls eventId={event.id} currentStatus={event.status || 'draft'} /> {/* Name Display Policy */} <div className="mb-4 md:mb-6 flex items-center gap-3"> <span className="text-sm text-secondary text-secondary">Attendee name display:</span> <NamePolicyBadge policy={(event as any).nameDisplayPolicy || 'attendee_choice'} /> <a href={`/${event.id}/edit`} className="text-xs text-imajin-orange hover:text-imajin-orange underline" > Change </a> </div> {/* Co-host Management */} <div className="mb-4 md:mb-8"> <CohostManager eventId={event.id} isOwner={isOwner} /> </div> {/* Invite Link Management */} <InviteManager eventId={event.id} accessMode={event.accessMode || 'public'} /> {/* Message Attendees */} <MessageComposer eventId={event.id} recipientCount={confirmedAttendeeCount} /> {/* Stats */} <div className="grid grid-cols-2 md:grid-cols-4 gap-2 md:gap-4 mb-4 md:mb-8"> <StatCard label="Tickets Sold" value={totalSold} /> <StatCard label="Revenue" value={formatRevenue(confirmedRevenue, heldRevenue, 'CAD')} /> <StatCard label="Checked In" value={`${checkedIn} / ${totalSold}`} /> <StatCard label="Event Date" value={new Date(event.startsAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })} /> </div> {/* Ticket Tiers */} <div className="mb-4 md:mb-8"> <h2 className="text-xl font-mono font-semibold mb-2 md:mb-4">Ticket Tiers</h2> <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-2 md:gap-4"> {tiers.map(tier => ( <div key={tier.id} className="bg-surface-surface bg-surface-elevated p-3 md:p-4" > <h3 className="font-semibold">{tier.name}</h3> <p className="text-2xl font-mono font-bold mt-2"> {tier.sold} <span className="text-sm text-secondary">/ {tier.quantity ?? '∞'}</span> </p> <p className="text-sm text-secondary"> {formatCurrency(tier.price, tier.currency)} each </p> </div> ))} </div> </div> {/* Guest List */} <GuestList eventId={eventId} isOwner={isOwner} /> </div> );
} function StatCard({ label, value }: { label: string; value: string | number }) { return ( <div className="bg-surface-surface bg-surface-elevated p-3 md:p-4"> <p className="text-sm text-secondary">{label}</p> <p className="text-2xl font-mono font-bold">{value}</p> </div> );
} const NAME_POLICY_LABELS: Record<string, { label: string; color: string }> = { attendee_choice: { label: 'Attendee choice', color: 'bg-info/10 text-info dark:bg-info/10 dark:text-info' }, real_name: { label: 'Real name', color: 'bg-success/10 text-success dark:bg-success/10 text-success' }, handle: { label: 'Handle only', color: 'bg-imajin-purple/10 text-imajin-purple dark:bg-imajin-purple/10 dark:text-imajin-purple' }, anonymous: { label: 'Anonymous', color: 'bg-surface-elevated text-muted bg-surface-elevated text-primary' },
}; function NamePolicyBadge({ policy }: { policy: string }) { const meta = NAME_POLICY_LABELS[policy] ?? NAME_POLICY_LABELS['attendee_choice']; return ( <span className={`text-xs font-medium px-2.5 py-1 rounded-full ${meta.color}`}> {meta.label} </span> );
} function formatCurrency(cents: number, currency: string): string { return new Intl.NumberFormat('en-CA', { style: 'currency', currency, }).format(cents / 100);
} function formatCurrencyRound(cents: number, currency: string): string { return new Intl.NumberFormat('en-CA', { style: 'currency', currency, minimumFractionDigits: 0, maximumFractionDigits: 0, }).format(cents / 100);
} function formatRevenue(confirmedCents: number, heldCents: number, currency: string): string { const confirmed = formatCurrencyRound(confirmedCents, currency); if (heldCents > 0) { return `${confirmed} (+${formatCurrencyRound(heldCents, currency)} pending)`; } return confirmed;
}
