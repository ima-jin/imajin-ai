'use client'; import { useState, useEffect, useCallback } from 'react';
import { apiFetch } from '@imajin/config'; interface Invite { id: string; eventId: string; token: string; label: string | null; maxUses: number | null; usedCount: number; expiresAt: string | null; createdAt: string | null; url: string;
} interface Props { eventId: string; accessMode: string;
} export function InviteManager({ eventId, accessMode }: Props) { const [invites, setInvites] = useState<Invite[]>([]); const [loading, setLoading] = useState(true); const [creating, setCreating] = useState(false); const [showForm, setShowForm] = useState(false); const [copied, setCopied] = useState<string | null>(null); // New invite form state const [label, setLabel] = useState(''); const [maxUses, setMaxUses] = useState(''); const [expiresAt, setExpiresAt] = useState(''); const [formError, setFormError] = useState(''); const fetchInvites = useCallback(async () => { try { const res = await apiFetch(`/api/events/${eventId}/invites`, { credentials: 'include' }); if (res.ok) { const data = await res.json(); setInvites(data.invites || []); } } finally { setLoading(false); } }, [eventId]); useEffect(() => { fetchInvites(); }, [fetchInvites]); async function handleCreate(e: React.FormEvent) { e.preventDefault(); setCreating(true); setFormError(''); try { const res = await apiFetch(`/api/events/${eventId}/invites`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include', body: JSON.stringify({ label: label || undefined, maxUses: maxUses ? parseInt(maxUses) : undefined, expiresAt: expiresAt || undefined, }), }); if (!res.ok) { const data = await res.json(); throw new Error(data.error || 'Failed to create invite'); } const data = await res.json(); setInvites(prev => [data.invite, ...prev]); setLabel(''); setMaxUses(''); setExpiresAt(''); setShowForm(false); } catch (err) { setFormError(err instanceof Error ? err.message : 'Failed to create invite'); } finally { setCreating(false); } } async function handleDelete(inviteId: string) { if (!confirm('Revoke this invite link? Anyone with this link will no longer be able to use it.')) return; const res = await apiFetch(`/api/events/${eventId}/invites/${inviteId}`, { method: 'DELETE', credentials: 'include', }); if (res.ok) { setInvites(prev => prev.filter(inv => inv.id !== inviteId)); } } function handleCopy(url: string, inviteId: string) { navigator.clipboard.writeText(url); setCopied(inviteId); setTimeout(() => setCopied(null), 2000); } const isInviteOnly = accessMode === 'invite_only'; return ( <div className="bg-surface-surface bg-surface-elevated p-6 mb-8"> <div className="flex items-center justify-between mb-4"> <div className="flex items-center gap-3"> <h2 className="text-xl font-mono font-semibold">Invite Links</h2> <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${ isInviteOnly ? 'bg-imajin-purple/10 dark:bg-imajin-purple/10 text-imajin-purple dark:text-imajin-purple' : 'bg-success/10 dark:bg-success/10 text-success text-success' }`}> {isInviteOnly ? 'Invite Only' : 'Public'} </span> </div> <button onClick={() => setShowForm(!showForm)} className="px-4 py-2 bg-imajin-orange hover:bg-imajin-orange text-primary text-sm font-semibold transition" > + Generate Invite Link </button> </div> {isInviteOnly && ( <p className="text-sm text-secondary text-secondary mb-4"> This event is invite-only. Only people with a valid invite link can purchase tickets. </p> )} {/* Create form */} {showForm && ( <form onSubmit={handleCreate} className="mb-6 p-4 bg-surface-elevated bg-surface-elevated/50 space-y-3"> <h3 className="font-medium text-sm">New Invite Link</h3> <div> <label className="block text-xs font-medium text-muted text-secondary mb-1"> Label (optional) </label> <input type="text" value={label} onChange={e => setLabel(e.target.value)} placeholder="e.g. VIP batch, For Sarah" className="w-full px-3 py-2 text-sm border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> <div className="grid grid-cols-2 gap-3"> <div> <label className="block text-xs font-medium text-muted text-secondary mb-1"> Max Uses (optional) </label> <input type="number" min="1" value={maxUses} onChange={e => setMaxUses(e.target.value)} placeholder="Unlimited" className="w-full px-3 py-2 text-sm border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> <div> <label className="block text-xs font-medium text-muted text-secondary mb-1"> Expires At (optional) </label> <input type="datetime-local" value={expiresAt} onChange={e => setExpiresAt(e.target.value)} className="w-full px-3 py-2 text-sm border border-white/10 border-white/10 bg-surface-surface bg-surface-elevated focus:ring-2 focus:ring-imajin-purple" /> </div> </div> {formError && ( <p className="text-xs text-error">{formError}</p> )} <div className="flex gap-2"> <button type="button" onClick={() => setShowForm(false)} className="flex-1 py-2 text-sm bg-surface-elevated hover:bg-surface-elevated dark:bg-surface-elevated dark:hover:bg-surface-elevated0 text-primary text-primary font-medium transition" > Cancel </button> <button type="submit" disabled={creating} className="flex-1 py-2 text-sm bg-imajin-orange hover:bg-imajin-orange disabled:bg-orange-300 text-primary font-medium transition" > {creating ? 'Creating...' : 'Create Link'} </button> </div> </form> )} {/* Invite list */} {loading ? ( <p className="text-sm text-secondary text-secondary py-4">Loading...</p> ) : invites.length === 0 ? ( <p className="text-sm text-secondary text-secondary py-4"> No invite links yet. Generate one above. </p> ) : ( <div className="overflow-x-auto"> <table className="w-full text-sm"> <thead> <tr className="text-left text-xs font-medium text-secondary text-secondary font-mono uppercase tracking-wider border-b border-white/10 border-white/10"> <th className="pb-2 pr-4">Link / Label</th> <th className="pb-2 pr-4">Uses</th> <th className="pb-2 pr-4">Expires</th> <th className="pb-2">Actions</th> </tr> </thead> <tbody className="divide-y divide-white/10 dark:divide-white/10"> {invites.map(invite => { const expired = invite.expiresAt && new Date(invite.expiresAt) < new Date(); const exhausted = invite.maxUses !== null && invite.usedCount >= invite.maxUses; return ( <tr key={invite.id} className={expired || exhausted ? 'opacity-50' : ''}> <td className="py-3 pr-4"> <div className="font-mono text-xs text-muted text-secondary truncate max-w-[200px]" title={invite.url}> ...{invite.token.slice(-8)} </div> {invite.label && ( <div className="text-xs text-secondary mt-0.5">{invite.label}</div> )} {expired && <span className="text-xs text-error">Expired</span>} {exhausted && <span className="text-xs text-imajin-orange">Exhausted</span>} </td> <td className="py-3 pr-4 text-muted text-secondary"> {invite.usedCount} / {invite.maxUses ?? '∞'} </td> <td className="py-3 pr-4 text-muted text-secondary"> {invite.expiresAt ? new Date(invite.expiresAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) : 'Never'} </td> <td className="py-3"> <div className="flex items-center gap-2"> <button onClick={() => handleCopy(invite.url, invite.id)} className="px-2 py-1 text-xs bg-surface-elevated hover:bg-surface-elevated bg-surface-elevated hover:bg-surface-elevated rounded transition" title="Copy link" > {copied === invite.id ? 'Copied!' : 'Copy'} </button> <button onClick={() => handleDelete(invite.id)} className="px-2 py-1 text-xs text-error hover:bg-red-50 dark:hover:bg-error/10 rounded transition" title="Revoke" > Revoke </button> </div> </td> </tr> ); })} </tbody> </table> </div> )} </div> );
}
