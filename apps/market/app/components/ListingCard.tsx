'use client'; import Link from 'next/link';
import { resolveMediaRef } from '@imajin/media';
import PriceDisplay from './PriceDisplay'; interface Listing { id: string; title: string; price: number; currency: string; category: string | null; images: unknown; sellerTier: string; status?: string; type?: string; createdAt: Date;
} function relativeTime(date: Date): string { const diff = Date.now() - new Date(date).getTime(); const minutes = Math.floor(diff / 60000); if (minutes < 1) return 'just now'; if (minutes < 60) return `${minutes}m ago`; const hours = Math.floor(minutes / 60); if (hours < 24) return `${hours}h ago`; const days = Math.floor(hours / 24); if (days < 30) return `${days}d ago`; return new Date(date).toLocaleDateString();
} function TierBadge({ tier }: { tier: string }) { if (tier === 'public_offplatform') { return ( <span className="px-1.5 py-0.5 text-xs font-medium bg-warning/10 text-warning border border-warning/30/50 rounded"> Direct </span> ); } if (tier === 'public_onplatform') { return ( <span className="px-1.5 py-0.5 text-xs font-medium bg-success/10 text-success border border-green-700/50 rounded"> Protected </span> ); } if (tier === 'trust_gated') { return ( <span className="px-1.5 py-0.5 text-xs font-medium bg-imajin-purple/10 text-imajin-purple border border-purple-700/50 rounded"> Trusted </span> ); } return null;
} export default function ListingCard({ listing }: { listing: Listing }) { const images = Array.isArray(listing.images) ? listing.images : []; const primaryRef = images.find((img): img is string => typeof img === 'string' && img.length > 0); const primaryImage = primaryRef ? resolveMediaRef(primaryRef, 'card') : undefined; const isSold = listing.status === 'sold'; const isRental = listing.type === 'rental'; return ( <Link href={`/listings/${listing.id}`} className="group bg-surface-surface border border-white/10 overflow-hidden hover:border-white/10 hover: hover:-black/40 transition-all duration-200 hover:scale-[1.01] flex flex-col" > {/* Image */} <div className="aspect-video bg-gradient-to-br from-gray-800 to-gray-700 relative overflow-hidden"> {primaryImage ? ( // eslint-disable-next-line @next/next/no-img-element <img src={primaryImage} alt={listing.title} className={`w-full h-full object-cover ${isSold ? 'opacity-60' : ''}`} /> ) : ( <div className="absolute inset-0 flex items-center justify-center text-muted"> <svg className="w-12 h-12" fill="none" viewBox="0 0 24 24" stroke="currentColor"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1} d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" /> </svg> </div> )} {isSold && ( <div className="absolute top-2 left-2 px-2 py-0.5 bg-red-600 text-primary text-xs font-bold rounded"> SOLD </div> )} </div> {/* Content */} <div className="p-4 flex flex-col flex-1"> <h3 className="text-primary font-semibold line-clamp-2 mb-1 group-hover:text-warning transition-colors"> {listing.title} </h3> <PriceDisplay price={listing.price} currency={listing.currency} className={`text-lg font-bold mb-2 ${isSold ? 'line-through text-secondary' : 'text-warning'}`} /> <div className="flex items-center gap-2 flex-wrap mt-auto pt-2"> {listing.category && ( <span className="px-2 py-0.5 text-xs bg-surface-elevated text-secondary rounded-full border border-white/10"> {listing.category} </span> )} <TierBadge tier={listing.sellerTier} /> {isRental && ( <span className="px-1.5 py-0.5 text-xs font-medium bg-sky-900/50 text-sky-400 border border-sky-700/50 rounded"> Rental </span> )} <span className="text-xs text-secondary ml-auto">{relativeTime(listing.createdAt)}</span> </div> </div> </Link> );
}
